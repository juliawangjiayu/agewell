# 照护协同智能体 · Demo 设计文档

**日期**：2026-08-05
**赛事**：AI CAN DO IT 腾讯云黑客松 · 新加坡站「Age Well」
**提交截止**：2026-08-09（约 4 天）
**关联文档**：`Age_Well_照护协同_项目方案.md`、`雇主访谈发现_20260805.md`

---

## 一、目标与主张

用一个网页版三人群聊，展示一个**照护判断引擎**：看得最久的女佣说一句话，系统决定这件事重不重要、该让谁知道、该怎么说；反方向，雇主布置一句含糊指令，系统拆成女佣能确认的逐项任务。

**要证明的核心命题**：产品的价值不在"语义理解 + 任务分解 + 跨语言生成"（这是通用大模型的商品化能力），而在其上的**判断层**——分级、相关方判定、差异化改写、确认闭环。这一层才是护城河。

---

## 二、范围

### 做（且只做）这三个场景 + 语音
1. **On-board**：雇主与女佣各自录入基础信息，Bot 建立家庭上下文库。
2. **故事 A · 女佣→雇主**：观察 → 分级/升级判断 → 相关方判定 → 差异化输出。
3. **故事 B · 雇主→女佣**：含糊指令 → 任务分解 → 跨语言生成 → 逐项确认闭环。
4. **语音**：女佣/雇主端语音输入（STT）；老人端语音输出（TTS）。

### 明确不做
趋势图、用药管理工具、预约挂号、多家庭管理、账号体系、老人端 App（老人只通过语音被动知情，不上屏操作）、跨人群校准。

> 纪律：demo 里出现的任何第二条判断链路都在稀释核心主张。新功能一律不加。

---

## 三、角色与界面

| 角色 | 语言 | 界面 | 说明 |
|---|---|---|---|
| 雇主 | 中文 | 三人群聊（可文字/语音输入） | 白天上班，数字能力强 |
| 女佣 Rosa | Tagalog（含 "eat small small" 式英菲混说） | 三人群聊（可文字/语音输入） | 菲律宾籍，沿用文档人设 |
| Bot | 双向 | 群聊内以第三方身份发言 | 判断引擎的对外形态 |
| 老人 Ah Ma | 中文（听） | **不上屏**，仅语音输出 | 82 岁，HDB，与女儿同住 |

**产品形态**：单页 WhatsApp 式三人群聊 Web 应用。老人不作为聊天参与者出现，只在触发升级/每周摘要时通过语音被告知。

---

## 四、架构

```
              ┌─────────────────────────────────────┐
   雇主(中文)─┤                                     │
              │   一个 Agent · 按发信人角色路由      │
   女佣(Tag)─┤                                     │
              │   ├── 女佣流：观察 → 分级 → 通知     │
              │   └── 雇主流：指令 → 拆解 → 确认     │
              └───────────────┬─────────────────────┘
                              │  共享
                    ┌─────────▼──────────┐
                    │   家庭上下文库      │  ← On-board 建立
                    │  (基线/用药/近期观察)│
                    └─────────┬──────────┘
                              │
                    ┌─────────▼──────────┐
                    │   判断层（强模型）  │  = 护城河 = "skill"
                    └────────────────────┘
```

- **一个 agent，两条流**：不是两套引擎。发信人是雇主走拆解流，是女佣走分级流，共用同一上下文库。这对应访谈结论"同一个判断层，跑两个方向"。
- **"女佣 skill / 雇主 skill" 是对外推广叙事**：便于宣传"谁都能接一个 AI API + 我们的 skill + WhatsApp 接口"。底层实现是单 agent 角色路由，不要让营销切分把架构搞乱。
- **判断层的护城河定位**：skill 本质是被产品化、可分发的结构化指令（prompt + 领域知识）。它是判断层的**分发形态，不是护城河本身**。护城河是 ① 具体的（未来经临床验证的）分级启发式；② 随时间积累的这个家庭的上下文库。**对评委的说法：skill 是我们判断层的分发机制，价值在里面装的判断，不在"能写 prompt"。**

---

## 五、家庭上下文库（数据模型）

On-board 阶段建立，两条流共享读取。

```
FamilyContext {
  elder:      { name, age, conditions[], baseline_notes }
  medications: [ { name, timing(饭前/饭后/睡前...), clock_time, notes } ]   // 占位
  followups:  { clinic, interval, next_date, last_med_change_date }         // 占位
  household:  { members[], who_manages_kitchen, primary_caregiver }
  helper:     { name, home_country, mother_tongue, care_abilities[] }
  recent_observations: [ { date, raw_text, restored_text, grade } ]         // 运行时累积
}
```

- `recent_observations` 是分级判断的关键上下文——同一句"没胃口"是第一次还是第三次，全靠它。
- `last_med_change_date` 是故事 A 升级判断的触发锚点。

---

## 六、场景一：On-board

### 雇主录入
- 老人基础信息：姓名、年龄、慢病。
- **用药表**：药名 · 饭前/饭后/时间（**占位数据，后续用真实 SG polyclinic 药名替换**）。
- 复诊节奏、最近一次调药日期。
- 家庭结构：几口人、谁管厨房、谁是主要照护者。

### 女佣录入 —— 用语纪律（重要）
录入她的**母语/来源国**和**基础照护能力**，但：

> **界面用语必须口语、暖场、不能出现"技能""考核""填报"这类词**——她可能根本听不懂"技能"。

用她的母语这样问：
- "你以前照顾过长辈吗？"
- "会不会帮忙翻身、量血压？"
- "平时会做哪些菜？"
- 整体框成"**帮你把这份工作做顺**"，不是一张能力评估表。

这直接对应访谈：会被接受的是"帮她把工作干好"，会被抵触的是"要求她定期填报/记录她做了多少"。**On-board 的第一印象就要立住"减负不监控"的定位。**

### 老人
不上屏，由雇主代录基础信息。

### 产出
Bot 汇总成 `FamilyContext`，并在群里用双语发一条温和的开场，确认信息已就位。

---

## 七、场景二：女佣→雇主 · 分级/升级判断（故事 A）

### 链路
```
Rosa 输入(Tagalog/混说)
  → ① 跨语言语义还原（"eat small small" → 进食量减少约 50%）
  → ② 上下文检索（用药史 / 近期观察 / 复诊节奏）
  → ③ 分级（记录 / 常规通报 / 即时升级）
  → ④ 相关方判定（子女 / 医生 / 都不通知）
  → ⑤ 差异化输出
```

### 三层分级（绝大多数落第一层是产品主张，不是能力不足）
| 层级 | 处理 | 预期占比 |
|---|---|---|
| 记录 | 不通知任何人，进上下文库 | ~85% |
| 常规通报 | 每日汇总，不打断 | ~12% |
| 即时升级 | 打断，定向送达 | ~3% |

### 高光屏："同一句话，不同结果"
输入固定为「Ah Ma 今天没什么胃口，午饭只吃了半碗」，并排展示上下文如何改变结论：

| 上下文条件 | 分级 | 通知对象 | 理由 |
|---|---|---|---|
| 孤立事件 | 记录 | 无 | 单次波动不构成信号 |
| 连续 3 天 | 常规通报 | 子女 | 持续性改变 |
| 三天前刚调药 | 即时升级 | 医生 | 时间关联提示药物反应 |
| 同时明显嗜睡 | 即时升级 | 医生+子女 | 症状组合优先级更高 |

**UI 必须显式打出「分级标签」+ 一行「为什么」**，让评委一眼看到这是上下文推理，不是关键词匹配、不是翻译 API。

### 差异化输出（同一事实，三种写法）
- **给子女**：口语、有解释、有具体动作、不制造恐慌。
- **给医生**：结构化、时间线、去情绪、标注"照护者观察，未经临床评估"。
- **给 Rosa**：一条明确的下一步 + 确认"这件事我会替你整理"。

---

## 八、场景三：雇主→女佣 · 指令拆解 + 确认闭环（故事 B）

### 链路
```
雇主输入（含糊、混着几件事的中文）
  例：「今晚四个人吃饭，妈妈的药记得饭前，6 点要炒菜就早点准备」
  → ① 语义理解
  → ② 任务分解（拆成独立可执行项）
  → ③ 跨语言生成（Tagalog）+ 挂上时间点
  → Rosa 端呈现为逐项任务
  → ④ Rosa 确认的不是「嗯」，是【逐项确认】
```

### 高光
把没有信息量的「嗯」变成**有信息量的逐项确认**。这是访谈刚验证过、最诚实、群聊里最直观的一屏。

对应访谈：断点是"她只会嗯嗯嗯——信息传了但没人知道有没有接住"。产品把"嗯"变成可核对的项：
- ☐ 今晚 4 人晚饭
- ☐ 妈妈的药 —— **饭前**
- ☐ 5 点开始备菜（6 点炒）← 体现"提前量"，对应访谈"5 点告诉她不要 5 点半"

### 纯 AI 才做得到
理解含糊、口语、混着几件事的中文指令，拆成可执行项，翻成她的语言并挂时间点——语义理解 + 任务分解 + 跨语言生成，规则/模板做不了。**但记住：可替代性的证明在"判断/拆解得对不对"，不在"会翻译"。**

---

## 九、语音

### 语音输入（女佣/雇主端）
- 群聊输入框支持按住说话，Web Speech API（STT）转文字后进判断层。
- 价值：强化"她说一句就行"，降低女佣打字门槛。

### 语音输出（老人端）
- 触发即时升级时：TTS 读出「这件事我需要告诉你女儿」——**告知，不请求许可**。
- 每周摘要：TTS 读出「这周你女儿知道了两件事：你胃口不太好，还有睡得比较晚」。
- 老人端设计原则（对应访谈修正）：老人妥协的动机是**保护子女、不想麻烦人**，所以老人端是**默认呈现的知情**，不是"需要主动行使的否决权"。批注/否决机制保留但不作主通道。

---

## 十、贯穿全程的定位纪律

| 维度 | 是 | 不是 |
|---|---|---|
| 对女佣 | 替她承担判断、帮她少犯错、出错前提醒 | 考核她汇报够不够、记录她做了多少 |
| 对老人 | 默认让她知道被看见了什么 | 需要她操作才生效的权利 |
| 对雇主 | 把成熟家庭的照护经验变成默认能力 | 一个监控女佣的工具 |

UI 文案和 Bot 话术都要体现，这是社会影响评分项。

---

## 十一、技术栈

- **前端 / 集成外壳**：腾讯 CodeBuddy / WorkBuddy 生态（比赛工具契合度）。
- **判断层**：调用强模型 API（承担分级、拆解、差异化改写、跨语言还原）。
- **语音**：Web Speech API（STT + TTS），浏览器原生，零额外服务。
- **上下文库**：单家庭内存态/轻量存储即可，无需账号体系。

---

## 十二、占位数据（需替换清单）

以下先用占位，后续用真实数据替换（不替换会在答辩被追问细节时露怯）：
- [ ] SG polyclinic 常用慢病药名与剂型
- [ ] 真实剂量与"饭前/饭后/几点"的时间安排
- [ ] Polyclinic 复诊/拿药周期
- [ ] Rosa / Ah Ma / 女儿的人设细节（可用访谈原话填充）

---

## 十三、Demo 剧本（一条时间线，一个人）

| 时间 | 事件 | 系统行为 |
|---|---|---|
| — | On-board | 雇主+女佣录入，建上下文库 |
| Day 1 | Rosa（语音 Tagalog）：今天午饭吃得少 | 记录，不通知 |
| Day 3 | Polyclinic 调整用药 | 更新上下文（`last_med_change_date`） |
| Day 4 | Rosa：还是没什么胃口 | 记录 + 建立关联 |
| Day 5 | Rosa：今天她说有点头晕 | **即时升级** → 医生+子女；老人端 TTS 告知 |
| — | 雇主：今晚四个人吃饭，妈妈药饭前，6 点炒菜 | 拆解 → Tagalog 逐项 → Rosa 逐项确认 |
| Day 7 | — | 每周摘要（TTS）+ 医生端一页摘要 |

**Day 5 是高光**：同一类输入（食欲），在有了用药上下文 + 症状组合后走向完全不同的结果。

---

## 十四、已知局限与验证路径（诚实标注，进 PPT）

- 分级阈值为初版，**未经临床验证**；依据是临床共识 + 一线经验。
- 单一家庭上下文库，无跨人群校准。
- 照护者观察本身有主观性。
- 女佣视角、老人视角的一手访谈仍缺失（本次访谈是雇主视角）。
- **可执行的验证设计**：回溯性对照——收集社区护士/Home Personal Care 实际探访中的升级决策，与系统对同期观察的分级做一致性比较。

> 坦承"这版没验证" + 给出可执行验证方案，比含糊带过强。

---

## 十五、4 天排期

| 天 | 内容 |
|---|---|
| D1 | 前端三人群聊 UI + On-board 表单（含女佣端用语纪律）+ 上下文库数据结构 |
| D2 | 判断层：故事 A（分级/升级）跑通，含"同一句话四种结果"对比屏 |
| D3 | 判断层：故事 B（拆解+逐项确认）跑通；接入 STT/TTS |
| D4 | 占位数据替换、Demo 剧本串排、录屏、PPT（重点第 4/5/7 页） |

**分级规则那一步不要外包**——那是唯一的护城河。
